buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        classpath("com.android.tools.build:gradle:8.2.0")
        classpath("org.jetbrains.kotlin:kotlin-gradle-plugin:1.9.0")
    }
}

// Loại bỏ allprojects repositories ở đây vì đã được định nghĩa trong settings.gradle
// Điều này sửa lỗi: "Build was configured to prefer settings repositories over project repositories"
