Here’s a summary of the key information from the “Generate Music” page of the Suno API (via KIE.ai) — so you can ask questions about it. ([docs.kie.ai][1])

---

## ✅ What is Suno API — Generate Music

* The endpoint for generating music (with or without lyrics) is a **POST** request to `/api/v1/generate`. ([docs.kie.ai][2])
* You must authenticate using a **Bearer Token** (API key) passed in the request header. ([docs.kie.ai][2])
* You send a JSON body describing what you want — prompt, style, model, etc. ([docs.kie.ai][2])
* The response (on success) returns a `taskId`, which you can use to track status and eventually retrieve generated audio. ([docs.kie.ai][2])

---

## 🧰 Key Request Parameters

Here are the main parameters you can specify — and when each is required:

| Parameter                                                                          | Purpose / Meaning                                                                                                                                                                                    |
| ---------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `prompt` (string)                                                                  | Description of desired audio. In non-custom mode: describes the general idea. In custom mode: if `instrumental = false`, prompt becomes the **lyrics**. ([docs.kie.ai][2])                           |
| `customMode` (boolean)                                                             | If **false**: minimal/simple mode — only `prompt` is used. If **true**: enables advanced control (style, title; plus lyrics if vocal). ([docs.kie.ai][2])                                            |
| `instrumental` (boolean)                                                           | Whether the output should be instrumental (no lyrics). Behavior depends on `customMode`. ([docs.kie.ai][2])                                                                                          |
| `model` (enum)                                                                     | Which AI model version to use. Options include `"V4"`, `"V4_5"`, `"V4_5PLUS"`, `"V4_5ALL"`, `"V5"`. ([docs.kie.ai][2])                                                                               |
| `style` (string)                                                                   | Desired music style/genre/mood (e.g. “Classical”, “Jazz”, “Electronic”). Required in custom mode. ([docs.kie.ai][2])                                                                                 |
| `title` (string)                                                                   | Title for the generated track — required in custom mode. ([docs.kie.ai][2])                                                                                                                          |
| (Optional) `negativeTags` (string)                                                 | List of styles or traits you want to exclude (e.g. avoid “Heavy Metal, Upbeat Drums”). ([docs.kie.ai][2])                                                                                            |
| (Optional, custom mode) `vocalGender` (string, “m” or “f”)                         | Preference for singing voice gender — but only increases probability, not guaranteed. ([docs.kie.ai][2])                                                                                             |
| (Optional) `styleWeight`, `weirdnessConstraint`, `audioWeight` (numbers 0.00–1.00) | Control how strongly the generation sticks to style (`styleWeight`), how “creative/experimental” it is (`weirdnessConstraint`), and balance audio-feature weight (`audioWeight`). ([docs.kie.ai][2]) |
| (Optional, custom mode) `personaId` (string)                                       | Apply a “persona” (custom voice/style) — requires previously created persona via the “Generate Persona” endpoint. ([docs.kie.ai][2])                                                                 |

**Character / length limits**:

* In custom mode: allowed prompt length varies by model (for V4: up to 3000 chars; for V4_5 / V4_5PLUS / V5: up to 5000 chars). ([docs.kie.ai][2])
* Style and title also have limits: style (e.g. for V4: 200 chars; for V4_5 / V5: up to 1000 chars), title: up to 80 characters. ([docs.kie.ai][2])
* In non-custom mode: prompt max length is 500 characters. ([docs.kie.ai][2])

---

## 🔁 Response & Workflow

* After POSTing, you get a JSON response containing e.g.:

````
{
  "code": 200,
  "msg": "success",
  "data": { "taskId": "some-id" }
}
``` :contentReference[oaicite:20]{index=20}  
- The generation process has several stages:  
   1. `text` — text generation (for lyrics or description)  
   2. `first` — first track variation ready  
   3. `complete` — all track variations complete :contentReference[oaicite:21]{index=21}  
- After completion you (or your server) should receive a callback to a URL you specified in `callBackUrl`, or you can poll status via a “Get Music Details” endpoint. :contentReference[oaicite:22]{index=22}  
- Generated audio files are retained temporarily (e.g. 14 days per Developer Notes) before deletion. :contentReference[oaicite:23]{index=23}  

---

## 📝 Recommendations / Good Practices (from docs)

- If you are new: start with **`customMode: false`**, send a simple `prompt`, and let the system auto-generate. That’s the easiest way to begin. :contentReference[oaicite:24]{index=24}  
- Use `negativeTags` to steer away from unwanted styles (e.g. if you don't want heavy drums or certain genres). :contentReference[oaicite:25]{index=25}  
- If you need lyrics or a specific style/structure, use `customMode: true`. Then supply `style`, `title`, and — if you want vocals — a `prompt` with lyrics. :contentReference[oaicite:26]{index=26}  

---

If you like — I can pull up a **full JSON schema** of the “Generate Music” request + response (with all fields, types, defaults, and limits).  
Do you want me to build that for you now?
::contentReference[oaicite:27]{index=27}
````

[1]: https://docs.kie.ai/suno-api/generate-music?utm_source=chatgpt.com "Generate Music - KIE API"
[2]: https://docs.kie.ai/suno-api/generate-music "Generate Music - KIE API"
